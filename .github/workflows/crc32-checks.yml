name: CRC32 checks

on:
  push:
    branches:
      - '**'
  pull_request:

jobs:
  verify:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run CRC32 verification script
        shell: pwsh
        run: ./checks/verify-crc32.ps1
